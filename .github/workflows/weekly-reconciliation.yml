name: Weekly Reconciliation (disabled)

# Previous version failed on every push due to YAML escape issues.
# Disabled until rebuilt cleanly. Manual trigger only.

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Disabled - rebuild later if needed"
